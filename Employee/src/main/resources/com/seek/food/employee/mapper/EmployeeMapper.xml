<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seek.food.merchant.Mapper.EmployeeMapper">

    <!-- 全局统一字段映射 -->
    <resultMap id="MerchantResultMap" type="com.seek.food.dto.Merchant.MerchantDTO">
        <result property="resign" column="is_resign" />
        <result property="delete" column="is_delete" />
    </resultMap>

</mapper>