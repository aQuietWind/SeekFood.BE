<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seek.food.meal.Mapper.MealMapper">

    <!-- 全局统一字段映射 -->
    <resultMap id="MerchantVoucherResultMap" type="com.seek.food.dto.Voucher.MerchantVoucherDTO">
    </resultMap>

<!--    新增餐品-->
    <insert id="insertMerchantVoucher">
        insert into meal (meal_id,merchant_id,meal_name,meal_price,meal_content)
        values(#{mealId},#{merchantId},#{mealName},#{mealPrice},#{mealContent});
    </insert>

<!--    获取餐品预览-->
    <select id="getSimple" resultMap="MerchantVoucherResultMap">
        select
            voucher_id,
            merchant_id,
            voucher_name,
            discount_cost,
            start_time,
            end_time,
            create_time
        from merchant_voucher
        where merchant_id=#{merchantId} order by voucher_id limit #{start},#{need}
    </select>

    <!--    根据类型获取餐品预览-->
    <select id="getSimpleEffective" resultMap="MerchantVoucherResultMap">
        select
            voucher_id,
            merchant_id,
            voucher_name,
            discount_cost,
            start_time,
            end_time,
            create_time
        from merchant_voucher
        where merchant_id=#{merchantId} and start_time &lt;= now() and end_time &gt;= now() order by voucher_id limit #{start},#{need}
    </select>

    <select id="getDetail" resultMap="MerchantVoucherResultMap">
        select
            voucher_id,
            merchant_id,
            voucher_name,
            voucher_description,
            discount_cost,
            min_cost,
            start_time,
            end_time,
            create_time
        from merchant_voucher
        where voucher_id=#{voucherId}
    </select>





</mapper>